modules:
  - type: files
    files:
      - source: home
        destination: /var/home # copies files/home/* (* means everything inside it) into your image's /var/home folder
      - source: system
        destination: / # copies files/system/* into your image's root filesystem